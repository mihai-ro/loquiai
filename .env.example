# Copy this file to .env for local development.
# In CI, set these as pipeline environment variables / secrets.

# Required when engine = "gemini"
GEMINI_API_KEY=

# Required when engine = "openai"
OPENAI_API_KEY=
