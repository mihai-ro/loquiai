{
  "name": "@mihairo/loqui",
  "version": "1.0.5",
  "description": "loqui — i18n translation engine for software projects.",
  "repository": {
    "url": "https://github.com/mihai-ro/loquiai"
  },
  "keywords": [
    "i18n",
    "translation",
    "language",
    "locale",
    "ai"
  ],
  "author": {
    "name": "Mihai Ro"
  },
  "license": "Apache-2.0",
  "main": "dist/lib.js",
  "exports": {
    ".": {
      "require": "./dist/lib.js",
      "import": "./dist/lib.js"
    },
    "./cli": "./dist/index.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "bin": {
    "loqui": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/build.mjs",
    "build:watch": "tsc -p tsconfig.build.json --watch",
    "test": "tsc && node --test 'dist/**/*.test.js'",
    "test:watch": "node --test --watch 'dist/**/*.test.js'",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm test && npm run clean && npm run build",
    "translate": "node dist/index.js"
  },
  "files": [
    "dist",
    ".loqui.example.json",
    "loqui.schema.json",
    "README.md"
  ],
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.5.0"
  }
}
